using System;

namespace Program1 {
    class test1 {
        static void Main() {

int x = Convert.ToInt32(Console.ReadLine());
Double y;
y = Math.Sqrt(Math.Abs(x)+Math.Sqrt(Math.Abs(x))+Math.Sqrt(Math.Abs(x)));

Console.WriteLine(y);
        }
    }
}

